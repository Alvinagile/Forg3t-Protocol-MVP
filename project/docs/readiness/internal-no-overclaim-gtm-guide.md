# Internal No-Overclaim GTM Guide

Rule 1: Always state proof boundary first.
Rule 2: Never escalate claim level above current evidence status.
Rule 3: If evidence is simulated/incomplete/blocked, say so explicitly.
Rule 4: API-only = governance/suppression/revocation scope unless vendor-level proof exists.
